# Hienfeld VB Converter - Docker/Production Requirements
#
# Excludes:
# - reflex (React frontend is primary UI)
# - pywin32 (Windows only)
# - CUDA/GPU libraries (CPU-only for smaller image)

# -------------------------
# Web / API
# -------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# -------------------------
# Data processing
# -------------------------
pandas>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# -------------------------
# Document parsing
# -------------------------
python-docx>=0.8.11
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# -------------------------
# OCR fallback (scanned PDFs)
# Note: Tesseract binary must be installed on the system
# -------------------------
pytesseract>=0.3.10
Pillow>=10.0.0

# -------------------------
# Fast string matching
# -------------------------
rapidfuzz>=3.0.0

# -------------------------
# NLP & Semantic Analysis (local, no API required)
# -------------------------
spacy>=3.7.0
scikit-learn>=1.3.0  # TF-IDF vectorization (2-3x faster than gensim)
wn>=0.9.0
# gensim>=4.3.0  # DEPRECATED: Replaced by scikit-learn for TF-IDF

# -------------------------
# PyTorch CPU-only (MUST be before sentence-transformers)
# Saves ~5GB by excluding CUDA/NVIDIA libraries
# -------------------------
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0

# -------------------------
# Sentence Embeddings (local)
# -------------------------
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4

# -------------------------
# AI / LLM (REMOVED - no external API dependencies)
# -------------------------
# openai>=1.0.0  # REMOVED: No external AI API calls needed

# -------------------------
# Rate limiting
# -------------------------
slowapi
