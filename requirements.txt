# Hienfeld VB Converter - Backend Requirements
#
# Core:
# - Analysis pipeline (hienfeld/)
# - FastAPI REST API (hienfeld_api/)
#
# The original Reflex UI is still supported for now, but the React/Vite
# frontend is the primary UI going forward.

# -------------------------
# Web / API
# -------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic-settings>=2.0.0
python-multipart>=0.0.6

# -------------------------
# Authentication (JWT + password hashing)
# -------------------------
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# -------------------------
# Data processing
# -------------------------
pandas>=2.0.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# -------------------------
# Document parsing
# -------------------------
python-docx>=0.8.11
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
pywin32>=306; sys_platform == 'win32'

# OCR for scanned PDFs (v4.6 - Tesseract fallback)
pytesseract>=0.3.10
Pillow>=10.0.0
# System requirement: tesseract-ocr + tesseract-ocr-nld (Dutch language pack)
# Linux: apt-get install tesseract-ocr tesseract-ocr-nld
# Mac: brew install tesseract tesseract-lang
# Windows: Download from https://github.com/UB-Mannheim/tesseract/wiki

# -------------------------
# Fast string matching
# -------------------------
rapidfuzz>=3.0.0
rank-bm25>=0.2.2  # v3.5: BM25 pre-filter for terminology matching in hybrid similarity

# -------------------------
# NLP & Semantic Analysis (local, no API required)
# -------------------------
spacy>=3.7.0
# Dutch model: python -m spacy download nl_core_news_md
scikit-learn>=1.3.0  # TF-IDF vectorization (2-3x faster than gensim)
wn>=0.9.0
# gensim>=4.3.0  # DEPRECATED: Replaced by scikit-learn for TF-IDF

# -------------------------
# Sentence Embeddings (local)
# -------------------------
sentence-transformers>=3.0.0  # Required for intfloat/multilingual-e5-large model
faiss-cpu>=1.7.4

# -------------------------
# AI / LLM (REMOVED - no external API dependencies)
# -------------------------
# openai>=1.0.0  # REMOVED: No external AI API calls needed

# -------------------------
# Rate limiting
# -------------------------
slowapi

# -------------------------
# Monitoring / Metrics
# -------------------------
prometheus-client>=0.19.0

# -------------------------
# Database (Fase 1.1)
# -------------------------
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0   # PostgreSQL driver
alembic>=1.13.0           # Database migrations

# -------------------------
# Testing
# -------------------------
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0    # For async tests
pytest-mock>=3.12.0       # Better mocking support
